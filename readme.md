基于bdragon300的上传客户端进一步封装，并对应于tusd协议进行修改的客户端。



对于tus/tusd协议，我在此基础上，增加了一个后处理，

## 1、对于上传单个文件：POST + PATCH。

POST请求可选地增加一个 请求头 upload-copy-path

然后会在PATCH请求合并单个文件后，把这个文件根据 服务端定义的 mapping.json 的映射关系，把合并后的文件拷贝到映射后的文件夹中。



## 2、对于Concatinate的方式

基于1 上传了 N个文件 ，然后发送一个 携带 isFinal请求头的请求 合并这几个文件。

## 3、example文件夹中提供了两个上传文件的方式 分别对应 1、2 中的方式



参考下 example下的两个写好的 函数